name: pipeline simple
on:
  push:
    branches: 
      -main
  pull_request:
    branches:
      -main
  workflow_dispatch:
jobs:
  build:
    runs_on: ubuntu-latest
    steps:
      - name: recuperer code
        uses: actions/checkout
      - name: Set up python
        uses: actions/setup-python
        with:
          Python-version: "3.11"
      - name: installer les dependencies 
        run: pip install -r requirements.txt
      - name: lint le code
        run: |
          flake8 app --count --select=E9,F63,F7,F82,F841 --show-source --statistics
          flake8 app --count --max-line-complexity=10 --max-line-length=127 --statistics
          flake8 tests --count --max-line-length=127 --statistics
      - name: lancer les tests
        run: pytest tests/ -v --cov=app